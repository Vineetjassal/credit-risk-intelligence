# CreditWatch Pages Dashboard

Static portfolio dashboard for GitHub Pages. It is intentionally separated from the Python/Streamlit application because GitHub Pages executes HTML/CSS/JavaScript only.

The displayed figures are synthetic demo data and must not be used for real credit decisions. The production architecture should publish precomputed, validated analytics from the Python pipeline rather than expose credentials or query SEC directly from the browser.
